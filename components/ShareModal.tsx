import React from "react";

const ShareModal = ({
  roomId,
  collaborators,
  creatorId,
  currentUserType,
}: ShareDocumentDialogProps) => {
  return <div>ShareModal</div>;
};

export default ShareModal;
